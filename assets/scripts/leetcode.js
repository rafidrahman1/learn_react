/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
  const arr= [];
  for(let i=0; i<nums.length-1; i++){
    let c=num[i]+num[i+1]
    if(c==target){
      arr.push(num[i]);
      arr.push(num[i+1]);
    }

  }
};